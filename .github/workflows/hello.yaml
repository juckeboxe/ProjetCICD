name: "say hello on each pull"
on: 
  push: # excecuted on all push
jobs: 
  say_hello: # first job declaration
    name: "say hello"
    run-on: ubunto-latest
    steps: 
      # The following step execute a shell script
      - name: "say hello"
        runs: |
            echos "Hello, word !" 
  dit_bonjour_a_jacob: # Premiere declaration
    name: "dit bonjour"
    run-on: ubunto-latest
    steps:
      # The following step execute a shell script
      - name: "dit bonjour"
        runs: |
            echos "bonjour jacob"
